"""Scoring engine: score(rubric, effective_values, floor_plan) -> {total, breakdown}.

Implemented in P0-3 against the pinned §9.3 breakdown contract
(manzil_shared.models.ScoreBreakdown), with golden tests in shared/tests/golden/.
"""
