# Phase 1 Status Assessment — July 09, 2026, 16:52

Snapshot of where Phase 1 (replace the spreadsheet, single user) stands, what is pending, and where the gaps are. Anchored at commit `16ca772` (jobs.hunt_id NOT NULL, migration 0005). Sources: IMPLEMENTATION.md §7 task table + changelog 2.0.16–2.0.21, DESIGN.md §19/§20, git history, and a code-level inventory of `api/`, `frontend/`, `worker/` — code was cross-checked against docs, not trusted from either alone.

## Bottom line

**Phase 1 is code-complete. P1-1 through P1-14 are landed and verified in code; the only remaining task is P1-15, the human-driven exit review.** All test suites are green against a live local database (worker 200 passed, api 18, shared 18, frontend vitest suites pass). No TODO/FIXME markers, no stubbed handlers (`NotImplementedYet` exists but is raised nowhere), no half-wired features found.

## Task-by-task state

| Task | State | Evidence |
|---|---|---|
| P1-1 migration 0002 + dev seed | ✅ Landed 2026-07-08 | changelog 2.0.17 |
| P1-2 queue mechanics + Postgres persistence | ✅ Landed 2026-07-08 | changelog 2.0.18; orphan-reclaim + atomic-projection tests |
| P1-3 in-process worker loop (lifespan) | ✅ Landed 2026-07-08 | changelog 2.0.19; `MANZIL_WORKER_INPROCESS=true` default |
| P1-4 API skeleton + auth chain | ✅ Landed 2026-07-07 | changelog 2.0.16 |
| P1-5..P1-8 all 16 handlers (hunts, rubric, listings, jobs, overrides, fees; rescore-on-mutation) | ✅ Landed 2026-07-09 | changelog 2.0.20; router integration tests + `test_e2e_flow` |
| P1-9 frontend shell (auth, routes, codegen) | ✅ Landed 2026-07-07 | changelog 2.0.16 |
| P1-10..P1-13 Overview table, detail drawer, rubric wizard, Tasks Active tab | ✅ **Landed in code 2026-07-08 (`467ae8c`) — but never recorded in the changelog** (see Gaps) | `OverviewTable.tsx`, `ListingDetailDrawer.tsx`, `RubricWizardPage.tsx`, `TasksActivePage.tsx` + vitest coverage |
| P1-14 `confirm_value` checkpoint round-trip | ✅ Landed 2026-07-09 | changelog 2.0.21; `test_confirm_value_checkpoint.py`; `CheckpointPromptCard.tsx` |
| **P1-15 exit review** | ⏳ **Pending — the only open Phase 1 task** | human-driven; no code remains |

Landed today (2026-07-09) on top of the above: migrations 0003 (`hunts.created_at`), 0004 (schema-alignment batch: `jobs.hunt_id`, `hunt_listings.unavailable_at`, `floor_plans` natural key), 0005 (`jobs.hunt_id` NOT NULL — which immediately caught the dev seed omitting it), dark mode (§20 entry), and the frontend read-model alignment. The 0004 batch's previously-unverified DB-backed tests have now run green against a real database (`supabase db reset` + full suites) — that pre-merge flag is cleared.

## Next steps, in order

1. **P1-15 exit review** — the actual Phase 1 gate. Its done-when checklist (IMPLEMENTATION.md §7):
   - Create Yusuf's real hunt through the UI; ingest the spreadsheet's listings via the API; build the rubric through the wizard.
   - Verify scores match hand-checked expectations.
   - Manual crash-recovery check: `kill -9` the API mid-job → job resumes from `current_stage` within `JOB_ORPHAN_AFTER_SECONDS` on restart.
   - Retire the spreadsheet; revise the Phase 2 table on entry (per §7's revise-on-entry rule).
   - Exit criterion: *a week of real use without opening Excel* — so this is a calendar gate, not just a checklist.
2. **Close the documentation drift** (small, do before or alongside P1-15): add the missing changelog entry for P1-10..13, and archive or delete `.claude/plans/status-on-phase-0-iridescent-canyon.md`, which describes the pre-execution state (says P1-1/P1-2/P1-3/handlers/dark mode are missing — all false since 2026-07-08/09) and will mislead any future session that reads it.
3. **Phase 0 tail (parallel, mostly human):** P0-11 bench labeling is the bottleneck — P0-12/13 code is landed but the eval harness and model bench can't produce meaningful runs until labels exist; P0-14 (census verdict + model pin → §20 entries) waits on P0-13. None of this blocks P1-15.
4. **Schema-review leftovers** (optional pre-exit, see Gaps): dead-letter cap at minimum — a week of real use is exactly when an uncapped retry loop would surface.

## Gaps

**G1 — The remaining schema-review items were never implemented.** The 2026-07-09 review proposed seven items; 0004/0005 shipped three (A `jobs.hunt_id` → now NOT NULL, §2 `unavailable_at`, D floor-plans natural key). Still open:
- **B: jobs dead-letter cap** — a repeatedly-orphaned job re-queues uncapped (`reclaim_orphans` has no attempt ceiling). Riskiest of the leftovers for P1-15's "week of real use".
- **C: `jobs.started_at`** — queue-wait vs run-time metrics; optional, cosmetic for now.
- **fee_checklist.updated_at** not bumped on re-entry — behavioral fix, still pending.
- **Unwired columns** (`place_id`, `resolution_rule`, …) — flagged in the review as "the real data-model weakness"; no code change yet.

**G2 — Changelog gap for P1-10..13.** The MVP frontend landed in `467ae8c` (2026-07-08) with follow-up polish/dark-mode commits, but IMPLEMENTATION.md's changelog jumps from 2.0.19 (P1-3) to 2.0.20 (P1-5..8) with no entry recording the frontend feature tasks. The task table's status is only derivable from git + code inspection — which contradicts the changelog's role as the authoritative landing record.

**G3 — Stale planning doc.** `.claude/plans/status-on-phase-0-iridescent-canyon.md` (labeled 2026-07-09) is a pre-execution plan whose "current state" section is now wrong on ~5 counts. Archive it or mark it superseded (this document supersedes it).

**G4 — Frontend test depth.** Unit/component vitest coverage only; no E2E or integration tests for the frontend (the API side has `test_e2e_flow`). Acceptable for Phase 1's single user — P1-15's manual pass is the de-facto E2E — but worth naming.

**G5 — Known-accepted Phase 1 gaps (by design, not defects):** no RLS (P2-1; ownership enforced in API dependencies — RLS is the real security boundary and it is OFF until Phase 2), jobs polling instead of Realtime (P2-4), no jobs History tab (P2-6), frontend direct-Supabase reads unscoped by RLS (closed by P2-1 backfill). Listed so nobody mistakes them for regressions during the exit review.

**Learning Track:** still L0 by rule — L1 stays gated on Phase 0's exit (§19), which is blocked on P0-11 labels → P0-13 runs → P0-14 decisions. No agents-mode code exists, correctly.
