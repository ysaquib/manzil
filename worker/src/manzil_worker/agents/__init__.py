"""Agents mode ONLY (DESIGN §10.11) — the learning track.

Nothing on the critical path imports from this package. This package imports
the deterministic truth layer and runner contracts; it never modifies them.
Empty until Learning Track L1 unlocks (after Phase 0 exit) — no agents-mode
code before then (CLAUDE.md, §19).
"""
