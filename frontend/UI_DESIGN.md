# Manzil — UI Design & Development

Working document for how the Manzil frontend **looks, feels, and is built**. It is the home for the
design philosophy and the **UI Decision Log** (bottom). It is deliberately practical: read it before
building or reshaping any surface.

**Where this sits in the hierarchy**

- `DESIGN.md §13` owns frontend **design intent** (what the surfaces are for). Authoritative on intent.
- Repo-root `AGENTS.md` + `frontend/AGENTS.md` own the **hard rules** (contracts, data layer, phase discipline).
- **This doc** owns the **craft**: visual language, component approach, consistency patterns, and a log of UI changes and why. When this doc and a `frontend/AGENTS.md` design bullet drift, this doc is the current word and the AGENTS bullet should be updated to point here.

---

## 1. Design philosophy

**"Dusk & clay."** *manzil* means home — a stage of a journey, arriving somewhere at dusk. The UI
should feel **warm, calm, and characterful**, not generic SaaS. (DESIGN §20, 2026-07-09.)

Guiding principles (Refactoring UI, adapted):

- **Hierarchy through weight, size, and warm-gray shade — not decoration.** Labels are lighter than
  data. De-emphasize secondary content instead of emphasizing everything.
- **Whitespace and grouping first.** Group with spacing and bounded **cards**; reach for divider
  lines sparingly.
- **Constrained, semantic palette.** Every color has a role and comes from the theme. Warm neutrals
  everywhere; one warm accent (clay); earthy status hues (moss/olive/ochre/brick). No electric colors.
- **Flat, not glossy.** Flat colors only — no gradients (backgrounds, buttons, badges, charts, score
  tiles). Depth comes from a restrained shadow scale and borders, not washes.
- **Serif for identity, sans for work.** Literata (serif) for headings/wordmark; Source Sans 3 (sans)
  for body; JetBrains Mono for figures/code. Tabular numerals wherever digits align in columns.
- **Plain-spoken, warm microcopy.** Empty/loading/error states speak plainly; controls say exactly
  what they do.
- **Data honesty over visual optimism.** Extracted, estimated, unknown, disputed, and human-overridden
  values remain distinguishable. Never make uncertainty quieter than the decision it can affect.
- **Design for the decision.** Dense screens should answer “what matters, what changed, and what can I
  do next?” at a glance. Progressive disclosure may hide supporting detail, never the current state,
  primary action, blocking error, or consequence of an action.

## 2. The theme is the single styling seam

- **`src/theme.ts` + `src/colors.ts` are the source of truth** for color, spacing, radius, shadow,
  typography, and component default props. Components consume Mantine props or CSS variables
  (`c="dimmed"`, `p="md"`, `shadow="sm"`, `var(--mantine-color-*)`) and **never hardcode hex**.
- Palette is defined once in `colors.ts` (named scales: `warm_stone`, `dark_dusky`, `dusky`,
  `sage`, `olive`, `muted_ochre`, `burnt_clay`, `dusty_brick`, etc.) and mapped to Mantine slots +
  score roles in `theme.ts`. Swap a role there, not per-usage.
- **Prefer the most semantic token available:** a component default or semantic role first, then a
  named Mantine palette value, then a raw Mantine CSS variable. Hardcoded color, spacing, radius,
  shadow, or ordinary font-size values inside a feature are the exception, not a parallel system.
- If the same visual choice appears across components, promote it to a component default, shared
  component, or named theme role. Do not copy an inline style or CSS declaration around the app.
- **Dynamic color** follows the `scoreColor`/`scoreBand` pattern: a pure function returns a Mantine
  **color name**, never a hex. Any new "color by value" logic does the same and is unit-tested.

## 3. Components: Mantine-first, CSS Modules where warranted

**Default to Mantine.** Compose from `@mantine/core` primitives (`Card`, `Stack`, `Group`, `Table`,
`Badge`, `Rating`, `Drawer`, `Popover`, `HoverCard`, …) and set look via **props + theme default
props**. Most surfaces need no custom CSS at all.

**Mantine primitives over raw structural HTML.** Use `Box`, `Stack`, `Group`, `Flex`, `SimpleGrid`,
`Paper`/`Card`, `AspectRatio`, `Text`, and `Title` instead of bare `<div>`, `<span>`, or `<p>` nodes.
Use Mantine's polymorphic `component` prop when the DOM needs a landmark or semantic element
(`Box component="section"`, for example). Raw HTML is reserved for a semantic element with no useful
Mantine equivalent (`pre`/`code` are common examples) or an integration that requires a literal DOM
node. It is not a shortcut for layout. Likewise, never use a styled `Box` as a button, link, input,
or table cell when the real interactive or semantic component exists.

**Use this styling order:**

1. **Theme defaults and component props** — `size`, `variant`, `color`, `gap`, `p`, `radius`,
   `shadow`, `visibleFrom`, and similar APIs.
2. **Mantine style props or a one-off inline `style`** for a root-only adjustment that belongs to
   exactly one instance. A single `miw={0}` or `style={{ fontVariantNumeric: "tabular-nums" }}` is
   clearer than creating a class solely for that declaration; a one-off type adjustment should stay
   on `Text` (`size="xs"` or, for the documented sub-`xs` exception, `fz="0.625rem"`).
3. **The component `styles` API** when one Mantine component needs adjustments to its internal slots.
4. **A co-located CSS Module** when selectors, pseudo-states, keyframes, complex layout, or several
   coordinated declarations make inline styling harder to understand.
5. **A shared component or theme rule** once a pattern repeats or becomes part of the product
   language.

Inline styling follows the same token rules as CSS. Do not create a class for one ordinary
declaration, and do not keep growing a dense `style={{ ... }}` object after the styling has acquired
states, selectors, or a reusable concept. Runtime-computed visuals should pass a token, Mantine color
name, or CSS custom property into a stable component style rather than generate arbitrary CSS.

**Don't hand-roll what Mantine ships.** Use the packaged component and restyle it to spec rather than
rebuilding it: `@mantine/carousel` for image/content carousels (never a bespoke scroll-snap strip),
`@mantine/dates` for date inputs, `@mantine/notifications` for toasts, `@mantine/modals` if a confirm
helper is wanted (raw `<Modal>` is already fine). One genuinely-custom exception in the drawer:
`ImageLightbox` stays hand-built because it is a full-screen **zoom viewer**, not a carousel.

**Reach for a co-located `*.module.css` file when** the design genuinely needs something the props
API doesn't express cleanly:

- bespoke layout (CSS grid tracks, `min-width:0` overflow control, sticky/pinned footers),
- state-driven visuals (pulse/shimmer/`data-*`-keyed styles, meters, sub-pips),
- fine control the theme can't reach (precise track/dot geometry).

Rules for module CSS (so it stays consistent and themable):

- **Colors are always tokens**, never raw hex: `var(--mantine-color-sage-6)` or a semantic variable
  such as `var(--mantine-color-default-border)`. `color-mix()` is acceptable for a genuinely derived
  tint when Mantine does not already expose the needed light/fill variable.
- **Use Mantine scales for nearly all visual values.** Prefer named component props in TSX and the
  matching variables in CSS or inline styles: `var(--mantine-spacing-*)`,
  `var(--mantine-radius-*)`, `var(--mantine-shadow-*)`, `var(--mantine-font-size-*)`, and
  `var(--mantine-font-family-monospace)`. This applies to margin, padding, gap, surface radius,
  elevation, and typography; snap to the nearest scale step instead of inventing a nearly identical
  value. Never override a themed `Card` radius or shadow with a bespoke value.
- **Sub-`xs` type is the narrow exception.** Use it only for nonessential micro-labels or constrained
  visualizations where Mantine `xs` cannot fit; it must not carry a primary action, form instruction,
  error, or decision-relevant value. Set it in `rem`, in `0.125rem` increments below `0.75rem`
  (`0.625rem`, then `0.5rem`) — never arbitrary values such as `0.68rem`. For larger text, use the
  Mantine text scale or an appropriate themed `Title`; if a new display size truly recurs, add it to
  the theme instead of hardcoding it in a feature.
- **Optical geometry stays explicit.** Dot/track/pip/avatar pixel sizes, `border-width`,
  `letter-spacing`, unitless `line-height`, and fixed grid-column widths are not spacing/font tokens.
  They may remain tuned px/rem values (see `PipelineTrack.module.css`/`HistoryCard.module.css`) when
  snapping to a space token would change the geometry's meaning. A focus or pulse ring expressed
  through `box-shadow` is also geometry; ordinary surface elevation still uses the shadow scale.
- **Theme-aware** via `:global([data-mantine-color-scheme="dark"]) .foo { … }` (no
  `postcss-preset-mantine`, so no `light-dark()` mixin). Audit both schemes.
- Keep modules **co-located** with their component and scoped to it, except a **composed surface** (e.g. Listing Detail Drawer) may own one token-driven `ListingDetailDrawer.module.css` imported by its dedicated child sections; each child still keeps unique layout/state geometry in its own module. No global stylesheets, no app-wide CSS layer, no inline hex in `.tsx`.
- Respect `prefers-reduced-motion`; give interactive chrome a visible focus state.

Reference implementations for an appropriate CSS Module boundary:
`features/rubric/CriterionViewCard.module.css` and `features/rubric/CriterionPicker.module.css`.

## 4. Consistency patterns (reuse these)

- **Section card.** A bordered, lightly-shadowed surface: serif title (order 5-ish) on the left, a
  quiet dimmed hint on the right, content below. Group related content into a few cards rather than
  many equal-weight sections.
- **Summary hero + detail.** For dense detail surfaces, lead with an at-a-glance hero (identity +
  the few headline stats, the leading stat visually strongest) then calmer detail cards. One scroll;
  nothing hidden behind tabs.
- **Score display.** `X / 15`, colored by `scoreColor`, labeled by `scoreLabel` (same bands). `✦`
  marks the exceptional (≥10) band. Additive breakdowns show a baseline row → signed deltas → total.
- **Quiet status markers over badges.** Prefer a small **colored dot** + a one-line legend, or a
  single `ⓘ` affordance with accessible detail, over stacking word-badges. Plum/grape =
  manual/human-entered (Overrides); sage/ochre/hollow = actual/estimated/unknown. Color reinforces
  the state but never carries it alone: pair it with text, shape, position plus a visible legend, or
  an accessible name.
- **Affordance hierarchy.** Frequent/important actions are always visible (primary `Button`,
  persistent controls). Rare/destructive actions are tucked but reachable (`Menu`, `Tooltip`/
  `HoverCard` for provenance, confirm `Modal` for destructive). Nothing important lives only behind a
  hover; nothing rare occupies prime real estate.
- **Complete state design.** Every data surface defines loading, empty, partial/stale, error, and
  success states. Preserve the existing layout during background refetches; use `Skeleton` for
  first-load structure, local errors with a relevant retry action, and purposeful empty states that
  explain the next step. Do not replace an entire usable page with a spinner for a local mutation.
- **Forms protect momentum.** Use Mantine inputs with visible labels, concise help only where needed,
  inline validation near the field, and explicit saving/saved/error feedback. Disable an action only
  when the reason is apparent; use confirmation for destructive or difficult-to-reverse actions, not
  routine edits.

## 5. Responsive, dark mode, accessibility

- **Phone-first (~375px) always.** Detail is a right `Drawer` on mobile and desktop; tables hide
  secondary columns or stack; forms/steppers stack. Prefer Mantine responsive props,
  `visibleFrom`/`hiddenFrom`, and theme breakpoints; use `useMediaQuery` only when behavior or rendered
  structure must change, not for ordinary styling. **No horizontal page scroll, ever** — wide
  content scrolls inside its own labeled container.
- **Dark mode via Mantine.** `defaultColorScheme="auto"`; header toggle persists `light`/`dark`
  (`manzil-color-scheme`); pre-mount script prevents flash. Contrast fixes go through `theme.ts`
  (`autoContrast`, `primaryShade`), not per-component hex. Audit every new surface in both schemes.
- **Interaction targets.** On touch layouts, primary controls and icon-only actions should provide a
  comfortable target (normally at least `2.75rem` in both dimensions), even when the visible icon is
  smaller. Keep adequate separation between adjacent destructive and routine actions.
- **Accessibility is structural.** Preserve heading order and landmarks; use real buttons, links,
  labels, lists, and table semantics; add accessible names to icon-only controls; keep focus visible;
  return or move focus sensibly when Drawers and Modals close; and announce meaningful async results
  through Notifications or an appropriate live region. Tooltip/HoverCard content must also work by
  keyboard and touch, and essential information must exist outside hover. Honor reduced motion.

## 6. Data & testing (pointers)

- Server state via TanStack Query only; render persisted `scores.breakdown` directly (never recompute
  client-side). Mutations invalidate keys. See `frontend/AGENTS.md` for the two-client split and
  `frontend/API_ASSUMPTIONS.md` for endpoint assumptions.
- **TDD.** Vitest + Testing Library tests live in the flat `frontend/tests/` directory. Test behavior
  through roles and accessible names; pure view-model/helper logic (`scoreColor`, `scoreLabel`, phase
  mappers, star-fill math) is unit-tested at boundaries. No snapshot tests. Visual changes are
  screenshot-verified at phone and desktop widths in both themes; keyboard-check every changed
  interaction.

## 7. UI review checklist

Before considering a surface complete:

- Mantine supplies the components and structure; any raw HTML or hand-built control has a concrete
  semantic or integration reason.
- Styling followed the escalation order, uses Mantine tokens, and introduces no repeated one-off
  values or arbitrary sub-`xs` typography.
- Hierarchy makes the primary information and next action obvious without hiding uncertainty,
  errors, or consequences.
- Loading, empty, partial/stale, error, success, and permission-appropriate states are intentional.
- The surface works at ~375px and desktop width, in light and dark schemes, with keyboard-only
  navigation, visible focus, and no color-only meaning.
- Behavior has focused tests; changed visuals have been inspected rather than approved from code
  alone.

---

## UI Decision Log

Newest first. Each entry: what changed, and why.

| Date | Change | Why |
|---|---|---|
| 2026-08-10 | **Alerts become a real third Account tab and a Hunt-profile section; Tasks carries the waiting count.** `/account/alerts` uses switches for the five account email defaults and states that invitations always send. Hunt → Your profile uses three-state selects — *Inherit — On/Off*, *On*, *Off* — so an override never hides what it is overriding; identity and alert edits share the panel's single sticky save bar. The Tasks nav item shows the known Checkpoint count in burnt clay and updates through the existing Jobs/Job Events Realtime invalidation. Pending Invites show delivery state beside expiry, retain the copy-link fallback, and expose resend only for terminal delivery failures; while a delivery is unsettled that list quietly polls every five seconds, then stops at a terminal state. | A preference control has to distinguish “off here” from “following my account”, and it must not ship before delivery exists. The known number belongs on Tasks because that is where the action is resolved; using the waiting hue makes it an interruption without borrowing the primary. Delivery state belongs on the Invite row rather than in a toast that disappears, and resend is exceptional recovery rather than a second everyday send button. The private outbox cannot safely be published to browsers merely to animate this row, so bounded active-only polling is the honest bridge. |
| 2026-08-10 | **An invitation clicked while signed out comes back to itself.** The auth guard redirects to `/login?next=<target>` instead of stashing the target in router state; the login page, the magic-link callback, the password-reset completion and `/signout` all carry it and all filter it through one allow-list (`/invite/…`, `/join/…`). While resuming, the login page reads **Sign in to accept your invitation** rather than *Welcome to Manzil*, and the invite page ends with *Joining as you@example.com · Use a different account*. Pending email invites gained a copy-link icon, and an invite whose email could not be sent now reports *Invite created, but the email wasn't sent* instead of a bare failure. Demo sessions now satisfy the route guards. | Router state does not survive a reload or a second tab, and the failure is invisible: you sign in, you are on the homepage, and nothing says the invitation was dropped. Telling people why they are on a sign-in form is the difference between a step and an interruption. Naming the joining account is the guard against the quiet wrong outcome — an invitation is addressed to one person and accepted by whoever this browser is signed in as. The copy-link icon exists because the email is best-effort and, for an address with no account, impossible; without a link the Owner had no fallback at all. |
| 2026-08-10 | **One deletion confirmation, and it names what it is about to take.** `ConfirmDeleteModal` (`src/components/`) backs every delete: a red consequence alert, then the subjects listed by name and detail in a bordered pane that scrolls at 220px instead of truncating, then — for an irreversible delete — a field that wants the subject's own name (one subject) or `permanently delete all selected <plural noun>` (several). Blocked subjects stay in the list, dimmed, with *Skipped — <reason>*, and a **N of M cannot be deleted** alert above the field. Reversible removals pass `requireTypedConfirmation={false}` and keep the list. Typed text is cleared whenever the selection changes. The Admin People roster gained a page-scoped select-all plus per-row checkboxes and a **Delete N accounts** bulk bar; refusals stay ticked after the run. The Hunt invite form is two rows at every width (email, then role + submit), and **Create invitation link** is a grey subtle button with a leading `+`. | A modal that says *are you sure?* asks about a count; a list asks about the things. Typing the name back is the one guard a mis-aimed click cannot pass, and it is worth its friction only where the act is permanent — so reversible removals keep the modal and lose the typing, which is what keeps typing meaningful. A blocked row is left visible because a filtered-out row reads as a bug. Two rows for the invite form because a three-up row squeezes the email to a dozen characters at 375px before it wraps anyway, and the quiet `+` button because minting a link is occasional while the links themselves are the content. |
| 2026-08-10 | **Admin → Demo Mode is a publication surface, not a settings switch.** The page leads with public/private state, selected Hunt, last publication time, and separate live/replay/map counts. Selecting or changing a Hunt opens an owned-Hunt picker, then a review step that inventories collaboration content, displays blockers/warnings, and requires the exact Hunt name. Publishing is visibly asynchronous; the old release stays public while building. A stale derived release gets one explicit **Publish updates** action. Turning Demo Mode off is immediate and retains the Hunt/release; publishing while off never turns it back on unless the first-selection flow explicitly says **Publish and enable**. | A toggle alone hides the consequential action: exposing a Hunt and its collaborators' history. The review makes the public boundary legible; exact-name confirmation prevents selecting the wrong Hunt by momentum. Manual refresh preserves operator review and controls map cost. Keeping live Listing counts separate from release-frozen replay/map counts explains why ordinary Overview edits appear immediately while archived/recoordinate changes wait. Retaining a disabled selection turns an emergency shutoff into a reversible operation rather than a teardown. |
| 2026-08-09 | **Permanent Listing deletion lives only in Archived and makes the actor reproduce the Property name.** Owner/Ghost View sees `Delete permanently` beside Restore; Curator/Member sees neither management action. The modal leads with permanence and the Property name, groups the impact counts instead of listing deleted content, states what survives globally, and requires an exact typed name before its destructive button enables. Active Jobs render a blocking alert. There is deliberately no bulk-delete control. | Archive is the safe, reversible normal action; exposing erasure in the Active row menu or bulk bar makes a destructive lifecycle decision too easy to trigger while comparing Unit Groups. Exact-name entry checks that the actor is looking at the intended Property, while the impact preview makes “as if it never existed for this Hunt” concrete without exposing deleted notes. Authorization remains in Postgres; these controls make the danger legible rather than serving as the boundary. |
| 2026-08-03 | **The admin People roster and account detail keep independent heights.** The two-column directory aligns both panels to the top; selecting a person may lengthen the detail column, but never stretches the roster card beside it. | The roster is a table, not a full-height sidebar. Stretching its border to match account actions created a large empty card whose size changed whenever the selection changed, making stable content look unstable. |
| 2026-08-03 | **The Hunt switcher gives Site Admins one primary-colour Admin panel card above their own Hunt cards.** The card uses the same `NavLink` shape and spacing as a Hunt, but a filled primary treatment distinguishes the account-level operator destination. It renders only after admin identity is confirmed. The Hunt list itself is explicitly membership-scoped, including for admins. | Ghost View read access should not turn an all-Hunts index into “Your hunts,” and the admin destination should be available before entering any Hunt. Reusing the established card shape keeps it part of the switcher while the filled primary treatment makes the different scope legible. |
| 2026-07-30 | **Custom Criterion authoring asks for intent once, then becomes an ordinary Rubric card** (P3-10, DESIGN v3.35). “Add custom criterion” opens one focused modal: name and plain-language question first; a routing suggestion then reveals the explicit confirmation controls for listing text or Maps, Property/Floor Plan scope, and yes/no, number, or controlled choices. Unsupported VISION/search suggestions stay visible with an explanation and cannot be confirmed until the Owner selects a supported route. After confirmation the Criterion uses the same option, points, Gate, bonus, enable, read-only ledger, and remove patterns as Catalog Criteria, grouped under one Custom heading rather than inventing a parallel editor. | Routing, scope, and truth shape change what evidence the pipeline may collect, so hiding them behind an automatic guess would make a cheap authoring mistake expensive and hard to diagnose. Once those acquisition semantics are confirmed, scoring semantics are already solved by the Rubric editor; visual parity keeps “custom” from reading as weaker or experimental truth. |
| 2026-07-30 | **P3-12 freshness is a quiet clock, not another status badge.** A stale Listing shows one neutral clock glyph in the existing Property marker slot, with the stale refresh classes named in its tooltip; active pipeline state still outranks it. “Refresh data” lives in the desktop row action menu and the Drawer Sources card, preserving the one-primary-action rule, and is disabled for viewers who cannot perform it. | Staleness matters when deciding whether to trust rent or availability, but it is neither a failure nor Hunt opinion. A quiet marker keeps the table calm, the tooltip carries exact scope, and putting the action in established overflow/detail surfaces avoids permanent per-row chrome. |
| 2026-07-28 | **Unmatched Floor Plan diagrams gallery** (P3-SC5, workbook §7.2) — `UnmatchedDiagrams.tsx`, its own `SectionCard` below Floor plans, rendering diagrams whose `floorPlanAssociations` is empty. Thumbnails are `fit="contain"` on a body-coloured surface rather than cropped to fill: cropping a diagram removes exactly the edge labels and dimension strings that make it worth showing. Clicking opens the existing `ImageLightbox`. **The card renders only when such diagrams exist** — an empty "unmatched" heading would imply a problem where there is none — and the copy states why they are here ("could not be tied to a specific plan… rather than attaching to every floor plan") instead of leaving the user to infer a bug. | The pipeline now recognises diagrams it cannot confidently attach to one plan, and there were only two honest places to put them: a Property-level gallery, or nowhere. Attaching one layout to every plan in the group is the failure this whole scoped-fact series exists to prevent, so the gallery is the surface that lets ambiguity stay visible without becoming a claim. |
| 2026-07-30 | **Ending a tour is a state, not a seal** (DESIGN §9.7). A completed Visit shows when it was toured and a **Reopen visit** action, because this feature is explicitly recorded on a phone and read on a desktop — and the desktop is where you fix the typo and finally type the answer you only half-heard in the hallway. Locking the record at End would have made the most reflective moment read-only. Reopening is an **explicit action with a visible state change** rather than quietly editable completed visits: collaborators see the pill move back to *In progress* instead of watching answers change under a *Completed* label. A cancelled visit offers no Reopen at all — reinstate comes first — so the UI never implies a transition the API refuses. | Nothing is rewritten in the dark: entries are append-only and every one is timestamped and attributed, so reopening cannot quietly alter the record of what was said on the day — a later edit simply reads as later. |
| 2026-07-30 | **The Visit score sits beside Fit and is never merged into it** (VC-8, DESIGN §9.7, §13.2). One column, out of 5, **starred** to mark it as human judgement rather than a computed one. Averaging it into the Rubric score would destroy the only comparison that matters: a listing that scores 7 on paper and 2.2 in person is exactly the row you want to notice, and a blended 4.6 hides it. A Unit Group nobody toured shows an **em dash, never a zero** — a zero reads as a terrible review rather than an absence. Where several doors in a group were toured the cell carries the same **×N** affordance the score cell already uses, and the tooltip states *why* this number and not another: best of N units, how many members rated it, when, and that it is never part of Fit. The drawer's Visits section lists every tour of the Property newest-first, starring the door that represents its Unit Group. That section is also where a **described unit matching no advertised Unit Group** stays visible: it can have no Overview row, because minting a Unit Group would contradict §3, but the tour still happened — the Overview's silence is a display rule, not data loss. | The colours are warm status hues (sage / olive / ochre / brick) rather than the Fit score bands, deliberately: two different scales sitting in adjacent columns must not look like one scale, or the reader will compare them as though they were. |
| 2026-07-30 | **A tour offers figures; it never writes them** (VC-7, DESIGN §9.7, §13.2). The ledger sits in the Money section, beside the questions that produce the numbers, and shows **what the listing currently believes next to the offer control** — the comparison is the whole point, so it reads the same composed figures the rest of the app shows rather than a private copy. An unknown line says **—**, never $0. Proposing says plainly that nothing changes yet; the decision belongs in Cost &amp; fees, where the person who owns the cost data is already looking. There, a proposal renders with the **staged-override hatch and dashed dot** an unsaved manual edit already uses, because that is exactly what it is, and carries **both ✓ and ✕ in a two-slot action area** — the single sanctioned exception to the one-icon rule (§13.2), since accept and reject are both primary and neither may hide behind a hover. Everywhere else the pencil/revert slot stays single. Someone without the cost-write permission still sees the offer and who confirmed it; they simply get no buttons, because knowing the agent quoted $85 is useful even to a member who cannot accept it. Rejecting never erases the visit's own record — a quoted figure and a declined one are two separate facts. | A monthly charge is offered against the unit you were standing in, but an application or admin fee against the building: attaching a one-per-property fee to a door would imply the next door has its own. Same reason accepting a base rent corrects that unit's Floor Plan rather than every layout — a proposal must not reach further than the same edit made by hand. |
| 2026-07-30 | **Answers that survive a dead signal, and forks a human settles** (VC-6, DESIGN §9.7). A tour happens in basements and concrete cores, so an answer appears the instant it is tapped and queues when the signal is gone; the badge says **"2 changes waiting to sync"** and its tooltip promises what the storage actually delivers — saved on this device, syncing by itself, nothing lost. It is silent when there is nothing pending, because a permanent "synced" chip is noise on a surface people glance at between rooms. When two people answered the same item and one was offline, the app **does not guess**: last-write-wins would quietly overwrite whoever walked into the dead spot, which is exactly the person whose answer was hardest to collect. The picker shows every distinct answer with who and when, in the member's own colour, and **defaults to the newest** — what the checklist is already showing — so confirming changes nothing by itself. A cleared answer reads as **Cleared** rather than a blank row, because the tombstone is a real choice. Branches that agree never appear at all: choosing between a value and itself would teach people to dismiss the one banner that must never be dismissed reflexively. | Caught in a 390px screenshot rather than by a test: the picker offered **"Problem"** beside a raw lowercase **"ok"**. A Check stores `ok`/`problem` but reads *Fine*/*Problem* on the tri-state control, and showing the stored token asks the reader to choose between an answer and a synonym for it. The lesson is narrow but keeps recurring — the words on a review surface must be the words the original control used, not the values underneath them. |
| 2026-07-30 | **Presence and attribution on a live tour** (VC-5, DESIGN §13.3, §9.7). Presence is **ambient**: an avatar row and one quiet sentence that names the section — "Sana is in Kitchen" — because on a tour the useful fact is not *that* someone is here but *where they are looking*, which is what stops two people surveying the same room. It never blocks, never prompts, and the checklist works with no signal at all. Beyond two people it counts rather than lists ("3 others are here"), since the strip has to fit a phone. Bylines appear **only on shared answers**: on a per-member item the owner is the author by construction, so attribution there would be noise, while on a shared answer "who last wrote this" is exactly the question a second reader has. Names use the member's own hunt colour — the same one their ratings and dots already carry — and your own writes read "You", because the byline exists to tell you when somebody *else* touched it. Team aggregates gained per-member dots for the same reason ratings were never merged: an average hides who disagreed. | A section change untracks before re-tracking, which blinks the member out of other people's avatar rows for a fraction of a second. That is deliberate: `track()` appends a presence meta and `untrack()` removes only one call's worth, so re-tracking without it strands a meta that no teardown can reach and the member shows as present **forever** after they leave. A sub-second blink is the right price for a roster that is actually correct. |
| 2026-07-30 | **Defect log and red-flag tally** (VC-4, DESIGN §9.7). The log's design claim is that it **fills itself**: marking a Check as a problem promotes into a defect carrying its unit and originating item, so the source document's defect table — the one nobody transcribes into mid-tour — never has to be typed. Promoted rows are badged *From a check*, and the empty state says how the log fills rather than just reporting emptiness. Three deliberate choices: a promoted defect starts **unrated** (`severity` nullable) because a number nobody chose is worse than no number, and clicking the current rating clears it back; `visit_unit_id: null` reads as **The building**, a value rather than a gap; and removing a defect is a **soft delete that leaves the Check's answer alone** — that a test failed and what the problem was are two separate records — with the trash tooltip saying so. Promotion is once-only per (visit, item, unit) including rows since deleted, so toggling a Check cannot resurrect something deliberately removed. **Red flags** are tallied per member ("Flagged by Yusuf, Sana") rather than merged, because they are judgements and a lone flag is information, not noise. | Found in the running app and fixed: the defect log silently lagged the checklist, because `useSaveVisitEntries` invalidated only the answers — but that write can create a row in *another* table. Every unit test passed while the UI showed "0 defects" over a defect that existed. The lesson generalises past this slice: **a mutation must invalidate what it can cause, not just what it obviously touches**, and cross-table effects are exactly what component tests with mocked hooks cannot see. Now covered by `visitApiInvalidation.test.tsx`, which asserts the invalidation contract directly rather than through a component. |
| 2026-07-30 | **Visit Checklist runtime** (VC-3, DESIGN §9.7). Four controls, one per entry kind, because the kinds differ in what they *mean*: a **Check** is tri-state where the middle state is "nobody has looked" rather than "false" (tapping the active state again clears it, appending a tombstone rather than deleting); a **Question** is answered by its typed text and nothing else, so *Mark asked* stays disabled until there is text and there is no way to tick one off without an answer; an **Impression** is keyed per member and shows a team average once more than one person has rated; a **Fact** renders from its `value_schema`. Answer identity — `(item, unit, owner)` — lives in the pure `entries.ts` and is re-derived nowhere, because getting it wrong merges two people's opinions or splits one person's answer across units. **Scope drives the chrome:** the unit switcher exists only on unit-scoped sections, and the scope bar states what is being recorded into. A **mixed** section — Building history asks about the building *and* about this unit — says "Mixed, each item says which" and tags every item with its own scope, rather than making a blanket claim that is false for half of it. The tri-state Check is a 2.75rem square on the thumb side (§5); text and Questions commit on blur or an explicit button so a save never fires per keystroke; a full-width control drops below its label via `:has(textarea)` rather than fighting it for room. | Two bugs found by driving the real app that the unit tests could not have caught, both recorded as regression tests: the section chips rendered as **empty boxes**, because Badge clips its own label and the chips were shrinking to zero inside a nowrap flex row (`flex: none` on a horizontal scroll strip is the fix — the strip scrolls, it does not compress); and the scope bar claimed "Recording into 4B" on a mixed section, which was simply untrue for its property-scoped questions. The second is the more important one: the whole point of declared scope is that an answer cannot be mis-filed, and a UI that lies about scope defeats it even when the write is correct. |
| 2026-07-30 | **Visits tab and create flow** (VC-2, DESIGN §9.7/§13.1–§13.2). A nav item between Compare and Rubric, three routes, and `features/visits/`. **State is never a column**: `visitState.ts` derives planned / in progress / completed / cancelled from `started_at`/`ended_at`/`cancelled_at`, with cancellation outranking everything — a tour that happened and was then called off is cancelled, not completed — and the same ordering runs server-side in `VisitResponse.state`, so the two cannot disagree. `VisitStatePill` is a dot plus the state **in words** (§4: colour reinforces, never carries), and only the live state animates, in a co-located module with a `prefers-reduced-motion` branch (§3's pulse case). **A cancelled visit stays in the list at 62% opacity with its reason**, because an agent no-show is information about the property and the prep survives a rebooking; the Cancelled filter only appears once something is cancelled. **Pre-visit is a state, not a screen**: creating navigates to the Visit itself, whose page opens only the property-scoped `prep` section and renders the other 21 sections as lock-iconed chips titled from a frontend `SECTION_TITLE` map — DESIGN §8.2 gives items a `section_key` and a globally monotonic `display_order` and deliberately no sections table, so grouping and order both fall out of the items. Lifecycle sends **actions, not timestamps** (`{action: 'start'}`), so a phone with a skewed clock cannot record a tour starting tomorrow. Cancel/delete sit in a kebab `Menu` and are disabled for anyone who is neither creator nor Owner, with a `Menu.Label` saying why — the API and a database trigger are the actual boundary (§4.2). `VisitConfirmDialog` was extracted for both destructive confirms rather than inlining two near-identical Modals. Stepper step **descriptions drop below `sm`** (they wrapped the stepper onto two rows and said nothing the label didn't), matching the settings rail's subtitle treatment. Units the listing never advertised carry a dashed teal outline everywhere they appear. | The tour checklist had no way in and no way to exist: a Visit could only be created by SQL. Deriving state rather than storing it removes a whole class of drift — a "completed" visit with no `ended_at` is unrepresentable — and gating sections on `started_at` means the prep questions are answerable on the couch while the room questions cannot be answered before anyone has seen a room. **Testing note for the next surface:** Mantine renders Combobox dropdowns, Menu dropdowns and Modals through `Transition`, which never settles under jsdom — their nodes carry the right roles but drift out of testing-library's visibility filter, and a `Menu.Item` click never reaches a `Modal` at all. Query those with `{ hidden: true }` and test dialog behaviour on the dialog component directly; both test files say so inline so nobody "fixes" it back. |
| 2026-07-28 | **Floor Plan detail surface** (P3-SC5 item 7). `FloorPlanPins`' radio group is replaced by a card list plus a nested detail modal: new `FloorPlanCard`, `FloorPlanDetailModal`, `FloorPlanList`, and the pure `floorPlanAmenities.ts`. **The modal is scoped to the drawer column on desktop** rather than the viewport — the Overview behind stays lit, so opening a plan never costs you your place in the hunt — and goes `fullScreen` below `sm`. That re-anchoring is the `styles` API, not a CSS Module, because Mantine's own `Modal-root`/`Modal-inner` rules beat a module class in the cascade and slot positioning has to be inline (§3 escalation step 3); two tests pin the desktop overrides and the mobile fork so a refactor can't silently return it to full-viewport. Because the overlay hides the drawer's own header, the modal header carries a **crumb** (`‹ 2 bd / 1.5 ba · N plans`) that closes back to the group and doubles as the “return to the Unit Group” affordance workbook §7.1 requires on mobile. **The card is a plain grid with a transparent stretched `UnstyledButton` over it and the pin `ActionIcon` lifted above on its own `z-index`** — a button cannot nest inside a button, and the pin has to stay independently focusable while the whole card opens the detail. Pin now lives in two places writing one draft: an `IconPin`/`IconPinFilled` toggle beside the score on the card (pin without opening) and a sticky modal footer button (pin after reading); the `Pinned` word-badge is gone from the card in favour of a leading dusk rule plus the filled icon (§4). The card also carries an **amenity count strip** (`✓5 ~1 ✕1 ?2`) so a group can be ranked without opening anything, with the words in a legend above the list so colour never carries meaning alone. Amenities render P3-SC4's **four** states — confirmed / advertised-unconfirmed / explicitly absent / unknown — with `unknown` as a dashed hollow chip, and scope tags (`this plan`, `all units`, `select units`) inside the chip so a Property-wide claim can't read as a plan fact. Evidence sits behind a per-chip `ⓘ` opening a single-open inline `Collapse`, not a `Popover`: a floating layer inside an overlay is one layer too many, and touch has no hover. The breakdown reuses `CriterionBreakdown` with `floorPlanId` set to **this** plan, never the group's display plan. Two honesty fixes ride along: Floor Plan diagrams are filtered out of the Property photo gallery (workbook §7.1 — a diagram must never stand in for a photo), and `heating` was missing from `overrides.ts`' `SCOPED_UNIT_CRITERIA`, so its legacy Property rows were still resolving. Draft: `claude.ai/code/artifact/2a9ca6ff`. | The alternatives list was a radio group: it could set a pin and show a score, and nothing else. Every plan-specific fact the scoped-Criteria work has been landing since P3-SC2 — amenities, applicability, evidence, per-plan breakdown, diagrams — had no surface to appear on, and a Unit Group's plans could only be compared by their score chip. Modal over expand-in-place because the content is a page, not a disclosure: five sections at ~600px in a 640px column, where an accordion puts each plan's chips at a different scroll offset and makes A/B comparison a re-finding exercise. |
| 2026-07-28 | **P3-6 disagreement presentation** — unresolved decision-relevant `conservative_disputed` facts add one red, quiet `Problematic` badge to the Overview row and drawer header. Criterion evidence shows the deterministic resolution rule and its Source candidates; `verified_positive_preferred` remains the quieter inline “sources disagree” treatment. Tasks History summarizes escalation rounds while preserving the complete manifest JSON. | A disagreement that can change a Gate needs a hunt-level interruption, while ordinary amenity disagreement should not turn the interface into a warning wall. Candidate provenance makes the deterministic winner inspectable. |
| 2026-07-28 | **Independent VISION confidence control** — the Hunt Settings scoring section places “Minimum VISION confidence” directly after the ordinary Extraction threshold. Its default option is labeled in the control and its description states the asymmetric consequence: low-confidence visual estimates remain useful for points/display but never supply Gate evidence. | A hidden backend exception would make the two thresholds impossible to reason about. Keeping them adjacent and naming the Gate consequence preserves the UI's data-honesty rule without adding a new visual pattern. |
| 2026-07-26 | **Map surfaces** — a `Location` card near the bottom of the Listing Detail Drawer and a `Map` tab at `/h/:huntId/map` (DESIGN §20 2026-07-26, `features/map/`). Both render Google Maps through `MapFrame`, which owns the four non-map states (no key / loading / failed / empty) so they can't drift apart. **Colour comes from the theme, not from the vendor**: marker fills, outline, and the dark basemap are read out of `--mantine-color-*` custom properties at paint time (`mapTheme.ts`), so score pins use the same `scoreColor` bands as the table and the chrome tracks `theme.ts` in both schemes. The pin outline **inverts against the basemap** — black on the pale light map, white on the charcoal dark one — rather than matching the page body, because the marker's job is to separate from the map beneath it, not from the surrounding chrome — the one place literal colours appear is the basemap `styles` array, which a raster map cannot take CSS variables for. No cloud Map ID, deliberately: that would move styling into a Google console and out of the repo. **Pin colour carries the honest fact, and only colour does**: one pin is one Listing, and a property whose Unit Groups land in different bands is *sliced* into equal sectors (best at 12 o'clock, hairline dividers) rather than averaged — the rule is the same one behind `scoreBand`/`scoreLabel`, that colour must never claim something the number doesn't. Solid and sliced pins are one silhouette at one size with one anchor; a test pins that equality. The marker is a single teardrop path (head and point share one continuous outline, and the slices are clipped to it so the point takes its share of the colour) — an earlier revision drew a circle plus a separate triangle with a hub dot on split pins, which clipped the point off, cut a visible arc across the neck, and made sliced pins read as a different kind of object. Clicking a multi-group pin opens a `Modal` picker (group label, rent range, band-coloured score badge) before the drawer, matching §9.4's rule that the Unit Group is the entity users curate. Pins whose data doesn't exist are stated, not hidden: an omission footnote counts "not scored yet" separately from "no mapped location". The drawer card doesn't repeat the address already in the drawer header. Filter state moved from `OverviewPage` into a hunt-keyed provider (`filterState.tsx`) so the Map and the table filter identically. | The app could only answer "where is this?" as a line of text. The two questions a map is actually for — where is this one, and where are all of them relative to each other — map exactly onto a card in the drawer and a tab of its own. Everything else was a refusal to spend: no new vendor, no new dependency, no geocoding, and no invented average score. |
| 2026-07-26 | **Overview table + row list redesign** (`OverviewTable`, new `OverviewRowList`, `OverviewFilterBar`). Grouped columns under a header band (Fit · Identity · Unit · Money · Timing · Place · Curation · People) with hairline group rules; selection/score/property pinned via `position: sticky` (module-owned stripe/hover, since Mantine paints those on `<tr>` with zero-specificity selectors a sticky `<td>` can't inherit) and a scroll shadow that only appears once a column is genuinely scrolled underneath (`data-scrolled`, toggled on `scrollLeft > 0`). **Pipeline hand-off**: a row still being fetched or whose last run failed shows a spinner/warning marker, states its stage or error inline, and links to `/tasks` or `/tasks?tab=history` (`rowState.ts`, `TasksPage` tab now URL-driven) instead of opening an empty drawer; a listing with no job at all is unaffected, and a scored group that's merely refreshing keeps its data and only swaps the marker. **Status**: the 140px `Select` + `Checkbox` pair becomes a `StatusChip` badge-menu + labelled `Visited` toggle (`interestStatus.ts` maps all ten statuses to a Mantine colour name — cyan/indigo/ochre/sage/brick/stone — and a Title Case label); the same tone drives a pinned dot in the identity cell (`RowMarker`, one slot, pipeline state outranks interest state) so status reads at every scroll position, not only when Curation is in view. **Ratings**: `RatingSummary` replaces five overlapping `Rating` widgets with a mono average + one member-coloured dot each + a comment count (hidden at zero); old `RatingDots` removed. **Filters**: `Filters` and the search box share one edge instead of a floating pill; 22 fields regroup into labelled sections in a `SimpleGrid`, paired bounds share a row with a dash, and small vocabularies (availability/status/visited/laundry/parking/pets/cooling/dishwasher) become `Chip.Group` toggles in Title Case (`titleCase` in `lib/text.ts`) — closed panel unmounts so its footer count can't linger in the accessibility tree. **Mobile**: below `48em` (`useMediaQuery`) the `<table>` — whose minimum width was the entire cause of a 701px-wide page at a 390px viewport — is replaced by `OverviewRowList`: collapsed rows show only score, property, unit, and all-in monthly; a borderless chevron expands to stat tiles (rent/sqft/available/deposit/city/added) then a full-width Status/People row then Listing/Compare/Archive actions; tapping the row still opens the drawer. Header renamed "All-In Monthly" (was "All-in / mo"). Presentation-only — `overviewRows.ts`'s pure row/sort/filter logic is unchanged. The stored column-picker key bumped to `-v2` since the redesign adds a `rent` column a v1 array wouldn't carry. Draft: `claude.ai/code/artifact/6f575fce`. | Measured before: `document.scrollWidth` 701px against a 390px viewport — the exact UI_DESIGN §5 violation. The table also spent its width on a flat one-fact-per-column spread with a dead gap at the right, hid curation behind permanent form chrome in every row, squeezed ratings into unreadable overlap at `maw={16}`, and gave no indication a row was still being fetched or had failed, silently opening an empty drawer for either. |
| 2026-07-25 | **Rubric categories re-grouped** (7 groups, none smaller than two criteria) and ordered by decision weight — The unit · Costs · Lease terms & rules · Location · Fittings & condition · Building & amenities · Management & service. Labels are the renter's words, so `catalogGroups.ts` no longer says "Floor plan and unit". The ruling and the full re-assignment live in **DESIGN §20 2026-07-25**: `category` is presentation vocabulary with no functional readers, so this is a label change made in `shared/catalog.py` + one sync migration rather than a frontend-only display map. Hue and icon-fallback maps follow the new vocabulary. | The redesign made the taxonomy's problem obvious once the groups were coloured: one 12-criterion drawer mixing a pool with an online rent portal, and two categories holding a single criterion while still costing a full heading and rule. Grouping by the question being asked survives catalog growth; an "other" bucket only grows. |
| 2026-07-25 | **Rubric cards redesign** (view + edit). (1) **Category hue**: `criterionCategory.ts` maps each catalog category to a Mantine palette name (slate blue/ochre/fog blue/sage/burnt clay/storm/dusty rose), rendered as a `ThemeIcon` identity tile on the card and a short rule under the group heading. Grape (human-entered) and dusty brick (danger/gates) are deliberately excluded so a tile can never read as a status. (2) **Points ledger**: signed mono tabular figures with a diverging magnitude bar scaled to the criterion's *own* largest delta (`deltaBar.ts`); dealbreaker rows show their set score and no bar. (3) **Markers**: `bonus` / `non-negotiable` word-badges → one tinted glyph each with the words on hover (§4). (4) **Unscored criteria** collapse from one empty card each into a single dashed add-pill strip per category (`CriterionPicker`). (5) Group headings moved from `Text fw={700}` to serif `Title order={4}`; grids no longer stretch cards to the tallest sibling; view mode goes to four columns at `xl`. (6) All 32 seeded catalog keys now have their own icon — twelve Property criteria had been falling through to the same house fallback — guarded by a test asserting no seeded key resolves to its category fallback. (7) JetBrains Mono actually installed (`@fontsource/jetbrains-mono`); `theme.ts` had named it since the identity pass while every figure silently fell back to system mono. Presentation-only: no contract, scoring, or API change. Editor page height 2205px → ~1840px *while showing every category instead of stopping at five*. Draft: `claude.ai/code/artifact/9b5aab9f`. | The rubric page spent its space on things that weren't being scored: 29 of 32 cards held nothing but a switch and a label, and the equal-height grid dragged a void beside every card that *was* scored. It also had no colour and no scanning aid — reading "which option actually moves the score" meant reading every digit. |
| 2026-07-25 | **Mantine-first authoring hierarchy and token discipline** (§§2–7). Mantine components and polymorphic primitives now precede raw structural HTML; styling escalates from theme/component props → one-off inline or style props → component `styles` → co-located CSS Module → shared theme/component. Colors, spacing, radii, shadows, and ordinary type use Mantine tokens; sub-`xs` type is limited to `0.125rem` increments. Added state, form, responsive, touch, accessibility, and review criteria; corrected test location to `frontend/tests/`. | The previous guide encouraged Mantine and tokens but left the everyday choice between inline styles and CSS Modules ambiguous, allowed off-scale type above `xl`, and treated accessibility/state coverage too narrowly. The explicit order keeps one-off styling lightweight without creating a second design system or class-per-declaration CSS. |
| 2026-07-24 | **Listing Detail Drawer shared CSS module** — exact cross-section primitives (header type, ledger state dots, band text colors, tabular nums, exceptional marker) live in `ListingDetailDrawer.module.css`; child sections import it for those tokens only and keep unique layout/tints locally. Mantine `Text`/`Box` pass on drawer children (no nested `<p>`). | Duplicated drawer tokens drifted across six modules; centralizing only byte-identical or intentionally shared rules cuts duplication without forcing unlike surfaces (fee vs all-in grids, plan chip colors) into one abstraction. |
| 2026-07-24 | **Drawer polish + Mantine-hardening pass** (`dev-phase3`). (1) Star ratings now take the member's color, yellow fallback (`starColorCss`). (2) `DrawerHero` recomposed from raw `<div>`s onto `Box`/`Text`/`Group`/`Title` primitives (typographic roles → props), output unchanged. (3) Token-anchoring tightened and swept across the drawer's module CSS — in-range spacing/font now use `var(--mantine-spacing-*)`/`var(--mantine-font-size-*)`; §3 rules updated. (4) The hand-rolled carousels (`DrawerImageGallery`, `PropertyImageCarousel`) rebuilt on **`@mantine/carousel`**; `ImageLightbox` kept custom (zoom viewer). (5) Tests relocated to the flat `frontend/tests/` dir. New §3 rules: *primitives over raw divs*, *anchor spacing/font to the scale*, *don't hand-roll what Mantine ships*. | Follow-up review of the shipped drawer: raw-div soup and off-scale pixel values were hard to maintain and couldn't be driven from `theme.ts`; a hand-rolled carousel duplicated a packaged component; star color carried no member identity. |
| 2026-07-24 | **Listing Detail Drawer redesign** (shipped; commits `300b48e..cc1d299` on `dev-phase3`, interleaved with an unrelated locality feature). Seven equal-weight `Section`s → a **summary hero** (name/address, large primary image + thumbnail filmstrip, score/all-in/beds tiles, score tile flat-tinted by band with a `✦` on ≥10) + **five topic cards** (Why this score · Cost & fees · Floor plans · Notes & ratings · Sources) built on the new `SectionCard`. Additive score tally (baseline 10 → signed deltas → `X / 15`); shared `scoreBand`/`scoreColor`/`scoreLabel` so color+label never disagree; badge declutter (state dots + one `ⓘ`/row + plum override dot); all-in result box; promoted sage utilities block; per-plan score chips; 5-star half-step ratings showing the whole team + average; compact multi-source rows. Presentation-only — no contract/scoring/data changes. Built via subagent-driven TDD (11 tasks, each spec+quality reviewed); 226 frontend tests + build + lint green. Spec: `docs/superpowers/specs/2026-07-23-listing-detail-drawer-redesign-design.md`; plan: `docs/superpowers/plans/2026-07-23-listing-detail-drawer-redesign.md`. | The drawer holds the most information and read as clutter (equal weight, stacked tables, badge pile-ups). It deserved a clear hierarchy: scan the headline, drill into calm grouped detail. |
| 2026-07-23 | **CSS Modules formally permitted** (Mantine-first, `*.module.css` where warranted; §3). Updates the older `frontend/AGENTS.md` "no bespoke CSS files" absolute, which the job-card work had already outgrown. The no-gradients rule remains in force. | Some layout/state visuals (pipeline track, meters, overflow control) can't be expressed cleanly through Mantine props; a scoped, token-driven module is clearer than prop gymnastics, without giving up theme consistency. |
| 2026-07-22 | **Job cards redesign** (Tasks page). Unified active/history cards on a shared five-phase `PipelineTrack`; status → colored dot + legend (fewer badges); history retry; expandable run-detail timeline; compact stop control on active cards. First substantial use of co-located CSS Modules. | The old cards showed raw stage text and lacked a retry path; the pipeline needed a legible, alive visual and a consistent language across active/history. |
| 2026-07-09 | **Visual identity: "Dusk & clay"** (DESIGN §20). Muted dusk-indigo primary, warm neutrals (stone light / charcoal dark), clay accent for waiting, earthy status hues, Literata + Source Sans 3 self-hosted, tabular numerals, sentence-case badges. All through `theme.ts`. | The UI read as generic SaaS defaults; *manzil* deserves warmth and character. Token-level identity is what the single theme seam exists for. |
