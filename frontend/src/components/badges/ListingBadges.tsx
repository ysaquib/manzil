// Inert badge slots for Phase 3 TTL/checkpoint semantics (Phase 1 plan §5.2).
// Wired into ScoreCell layout now; render null until data exists.

export function StaleBadge() {
  return null;
}

export function AutoResolvedBadge() {
  return null;
}

export function SingleSourceBadge() {
  return null;
}
