import { PagePlaceholder } from "../../components/PagePlaceholder";

// Overview table: one row per Unit Group, score cells, filters (P1-10).
export function OverviewPage() {
  return <PagePlaceholder title="Overview" task="P1-10 (Overview table)" />;
}
