import { PagePlaceholder } from "../../components/PagePlaceholder";

// Tasks Active tab: polls GET /v1/hunts/{id}/jobs at 3s (P1-13).
export function TasksActivePage() {
  return <PagePlaceholder title="Tasks" task="P1-13 (Tasks Active tab)" />;
}
