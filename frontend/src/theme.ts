// Mantine theme (IMPLEMENTATION §2: Mantine components only, no bespoke CSS
// until something forces it). Kept minimal at scaffold time.
import { createTheme } from "@mantine/core";

export const theme = createTheme({
  primaryColor: "indigo",
  fontFamily:
    "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif",
});
